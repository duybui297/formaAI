---
phase: 03.2-pdf-table-formula-fidelity
verified: 2026-04-26T23:00:00Z
status: human_needed
score: 2/3
overrides_applied: 0
human_verification:
  - test: "Re-run translation on BMC academic-paper job (932530eb fixture) with Phase 03.2 code active"
    expected: "Table 2 row cells are distinct segments in the review UI; page-3 and page-10 math/symbol glyphs render without tofu/missing-glyph boxes in the output PDF"
    why_human: "find_tables() produces 0 table_cell segments for the BMC fixture (text-drawn borders not detected by PyMuPDF default strategy). The table infrastructure works on synthetic vector-bordered PDFs but the end-to-end fix for the BMC paper's Table 2 requires either a re-run with strategy='text' or a manual inspection of the output PDF for the actual UAT fixture job"
---

# Phase 03.2: PDF Table + Formula Fidelity — Verification Report

**Phase Goal:** Close the 2 architectural PDF gaps after Phase 3.1 UAT — table-row cell fusion and math/symbol-font glyph corruption.
**Verified:** 2026-04-26T23:00:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Tables detected via `page.find_tables()` produce per-cell segments (one per cell) with kind='table_cell'; reassembly redacts and reinserts each cell into its own bbox preserving column structure | VERIFIED | 15/15 extractor tests pass including 4 new table-cell TDD tests; synthetic 2×3 table produces 6 `table_cell` segments with positions `page.0.table.0.row.R.col.C`; reassembler test `test_table_cell_segment_reassemble_no_crash` passes; spot-check confirms `kind='table_cell'`, correct structural_position, correct source_text per cell |
| 2 | Spans whose font matches math/symbol passthrough list (`AdvP*`, `CMSY*`, `STIX*`, `MathFont*`, etc.) are flagged as `glyph_passthrough` and emitted unchanged — no redact, no reinsert, original glyphs visible | VERIFIED | `_is_math_font('AdvP4C4E74')` returns True; `_is_math_font('AdvTT86d47313')` returns False; BMC paper page 3 produces 6 `math_passthrough` segments from AdvP4C4E74/AdvP4C4E51/AdvP4C4E46 fonts; reassembler `test_math_passthrough_segment_is_not_redacted` passes; spot-check confirms source text survives redaction skip; worker pre-seeds `translated_map[seg.id] = seg.source_text` for passthrough segments, excluding them from LLM batches |
| 3 | UAT items pertaining to Table 2 parsing and page-3/10 formula rendering on job `932530eb` move from issue to resolved on the BMC academic-paper test fixture | NEEDS HUMAN | BMC paper page 3 math-passthrough is confirmed working (6 passthrough segments, AdvP fonts detected). However, `find_tables()` detects 0 tables in the BMC fixture (Table 2 uses text-drawn borders, not line-art primitives — known PyMuPDF limitation). The infrastructure works on vector-bordered synthetic tables. SC3 requires end-to-end UAT re-run to confirm the fix is visible in the translated PDF output. |

**Score:** 2/3 truths verified

### Note on SC1 (Table Infrastructure vs. BMC Fixture)

The prompt explicitly notes: "The infrastructure (per-cell segment emission, kind-aware reassembly, worker passthrough filter) is in place and unit-tested; the BMC fixture skip is informative not regressive. SC1 should be assessed at the code-level (path is wired and works for vector-bordered tables) with a noted gap that text-positioned tables need a strategy='text' fallback in a follow-up."

SC1 is rated VERIFIED because the code path is fully wired and passes all unit tests on synthetic vector-bordered tables. The BMC table limitation is a known architectural gap (documented in the SUMMARY and skip message) — not a missing feature. SC3 is NEEDS HUMAN because end-to-end UAT on the actual fixture has not been re-run since Phase 03.2 was applied.

---

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `backend/src/app/pipeline/segment.py` | `Segment.kind` field — Literal\['text', 'table_cell', 'math_passthrough'\] with default 'text' | VERIFIED | Line 44: `kind: str = "text"  # "text" \| "table_cell" \| "math_passthrough"`; `from_text` accepts `**kwargs` passing `kind` through |
| `backend/src/app/pipeline/pdf/extractor.py` | `_is_math_font(font_name) -> bool` and math-passthrough block emission; `extract_pdf_segments` with `find_tables()` cell walk | VERIFIED | `_is_math_font` at lines 52-76; math-passthrough block at lines 301-318; `find_tables()` cell walk at lines 229-261; non-table block filter at lines 265-271 |
| `backend/src/app/pipeline/pdf/reassembler.py` | kind-aware dispatch: text (existing), table_cell (cell bbox lookup), math_passthrough (skip) | VERIFIED | `math_passthrough` skip at line 175-176; `table_cell` synthetic block path at lines 178-204; `find_tables()` cell bbox recovery at lines 149-164 |
| `backend/src/app/workers/translate_worker.py` | math_passthrough branch — skips translation, stores source_text as translated_text identity | VERIFIED | `_translatable` filter at line 344; `translated_map` pre-populate at lines 399-401; `batch_seg_groups` indexes `_translatable` at line 409; ORM insert sets `translated_text=source_text` for passthrough at line 362; WR-02 kind guard at lines 618-622 |
| `backend/tests/pipeline/test_pdf_extractor.py` | TDD tests: Segment.kind field, _is_math_font, table cell extraction | VERIFIED | 15 tests total (7 pre-existing + 4 Plan-01 TDD + 4 Plan-02 TDD); all 15 pass |
| `backend/tests/pipeline/test_pdf_reassembler.py` | Tests for math_passthrough skip and table_cell redact behavior | VERIFIED | 10 tests total (8 pre-existing + 2 new); all 10 pass |
| `backend/tests/pipeline/test_pdf_roundtrip.py` | Integration test using BMC fixture — Table 2 cell count + page-3 passthrough segment count | PARTIALLY VERIFIED | `test_bmc_paper_page3_math_passthrough_segments` PASSES; `test_bmc_paper_table2_produces_cell_segments` SKIPS with clear informative message (find_tables detects 0 tables on BMC fixture) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `extractor._is_math_font` | `Segment.kind='math_passthrough'` | `extract_pdf_segments` sets kind='math_passthrough' when `_is_math_font` returns True | WIRED | Pattern `kind.*math_passthrough` confirmed at extractor.py:314; spot-check on BMC fixture: 6 passthrough segments from AdvP fonts |
| `Segment.kind='table_cell'` | `reassemble_pdf` kind-aware dispatch | Reassembler checks `seg.kind == "table_cell"` and uses cell bbox from `find_tables()` | WIRED | Pattern `table.*row.*col` confirmed in `table_cell_bboxes` key construction at reassembler.py:160; test `test_table_cell_segment_reassemble_no_crash` passes |
| `Segment.kind='math_passthrough'` | `reassemble_pdf` skip | `if seg.kind == "math_passthrough": continue` excludes from `active_pairs` | WIRED | Pattern `math_passthrough` at reassembler.py:175; behavioral spot-check confirmed source text survives |
| `translate_worker._translatable filter` | `pack_into_batches` | Only non-passthrough segments packed into LLM batches | WIRED | `_translatable = [s for s in segments if getattr(s, "kind", "text") != "math_passthrough"]` at worker.py:344; `batch_seg_groups` indexes `_translatable` at worker.py:409 |
| `translated_map[seg.id]` | `reassemble_pdf` | Passthrough segments pre-seeded with identity translation before gather | WIRED | Pre-populate loop at worker.py:399-401; ORM insert also sets `translated_text=source_text` for passthrough at worker.py:362 |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|-------------------|--------|
| `extract_pdf_segments` — table_cell path | `raw` (cell text) | `page.get_textbox(cell_rect)` — live PyMuPDF clip | Yes — clips to cell bbox, returns actual cell text | FLOWING |
| `extract_pdf_segments` — math_passthrough path | `raw_text` (block text) | `span.get("text", "")` from `page.get_text("dict")` spans | Yes — raw span text from PDF layer | FLOWING |
| `reassemble_pdf` — table_cell path | `cell_rect` (bbox) | `page.find_tables()` re-run on same source doc | Yes — real cell bboxes from live table detection | FLOWING |
| `translate_worker` — passthrough pre-seed | `translated_map[seg.id]` | `seg.source_text` (identity copy) | Yes — source text from extraction, not empty | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| `Segment.kind` defaults to "text" | `Segment.from_text('hello', 'pos', 0).kind` | `'text'` | PASS |
| `Segment.from_text` accepts kind='math_passthrough' | `Segment.from_text(..., kind='math_passthrough').kind` | `'math_passthrough'` | PASS |
| `_is_math_font('AdvP4C4E74')` | Python spot-check | `True` | PASS |
| `_is_math_font('AdvTT86d47313')` | Python spot-check | `False` — AdvTT is body font, not math | PASS |
| `_is_math_font('Arial-BoldMT')` | Python spot-check | `False` — MT suffix not same as MT prefix | PASS |
| Synthetic 2×3 table produces 6 `table_cell` segments | `extract_pdf_segments` on vector-bordered table | 6 segments, kind='table_cell', positions page.0.table.0.row.R.col.C | PASS |
| `math_passthrough` reassembler skip | `reassemble_pdf` with all segs forced to passthrough | Output contains source text, "REPLACED" not found | PASS |
| BMC page 3 AdvP fonts → passthrough | `extract_pdf_segments` on BMC fixture page 3 | 6 passthrough segs, 8 text segs on page 3 | PASS |
| BMC Table 2 cell detection | `find_tables()` on BMC fixture | 0 tables detected — text-drawn borders not recognized | SKIP (known limitation) |
| Full pipeline suite | 115 passed, 1 skipped, 0 failed | 115/116 | PASS |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|------------|-------------|--------|---------|
| PDF-02 | 03.2-01, 03.2-02, 03.2-03, 03.2-04 | Translated PDF via redact-annot + insert_textbox with Noto CJK | SATISFIED | Reassembler kind-aware dispatch preserves existing redact-reinsert for text kind; table_cell uses synthetic block for same path; math_passthrough skips both |
| PDF-03 | 03.2-01, 03.2-02, 03.2-03, 03.2-04 | Overflow detection when text doesn't fit bbox | SATISFIED | `_MIN_RECT_HEIGHT_PT` and `_MIN_RECT_WIDTH_PT` guards applied to both text and table_cell rects; overflow_flags emitted for undersized cells |
| LAYOUT-02 | 03.2-01, 03.2-02, 03.2-03, 03.2-04 | Format-specific overflow detectors run post-translation | SATISFIED | PDF bbox overflow detection via `spare_height < 0` from `insert_htmlbox(scale_low=0.7)` unchanged; additionally, table_cell undersized rect detection added |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `backend/src/app/pipeline/pdf/extractor.py` | 257 | `import logging as _logging` inside `except` block | Info (IN-02 from REVIEW.md) | No functional impact — Python caches module imports; inconsistent with rest of codebase using structlog. Review noted but not blocking. |
| `backend/tests/pipeline/test_pdf_roundtrip.py` | ~132 | `import os`, `import re`, `from pathlib import Path` mid-file | Info (IN-03 from REVIEW.md) | PEP 8 / ruff import ordering; no functional impact. |

Both info-level anti-patterns were documented in the code review (03.2-REVIEW.md) but were out of scope for the review-fix pass (03.2-REVIEW-FIX.md only fixed WR-01 and WR-02). Neither is blocking.

### Human Verification Required

#### 1. BMC Academic-Paper End-to-End Re-Run

**Test:** Submit a translation job on the BMC academic-paper PDF (`.data/jobs/932530eb-eeda-4b0a-986a-7d32f74fa820/source.pdf`) against the Phase 03.2 backend. Use any source→target language pair.

**Expected for math/symbol glyphs (SC3 — page 3):**
- Open the translated PDF in any viewer (Acrobat, Preview, Evince)
- Page 3 glyphs rendered with `AdvP4C4E74` font (quarter-fraction `¼` and similar symbols) should remain visible as the original glyphs — no tofu boxes, no missing-glyph rectangles
- Body text on page 3 (rendered with AdvTT* fonts) should be translated to the target language

**Expected for Table 2 (SC3 — page 6):**
- In the Review UI, segments from Table 2 may still appear fused (find_tables detects 0 tables on this fixture due to text-drawn borders). This is the known limitation. Verify that the page does not crash and that body text is translated.
- Alternatively: if a `strategy="text"` fallback is added to extractor.py for text-positioned tables, re-run and confirm Table 2 rows produce distinct cell segments.

**Why human:** `find_tables()` detects 0 tables on the BMC fixture (text-drawn borders, not line-art), so the table-cell infrastructure cannot be exercised end-to-end on this fixture via automated testing. The math-passthrough improvement on page 3 CAN be verified end-to-end by a human inspecting the output PDF.

---

## Gaps Summary

No hard gaps — all required code infrastructure is in place and passes automated testing. SC3 requires human re-run on the BMC fixture to confirm the math-passthrough improvement is visible in the actual translated PDF output.

The table-cell extraction limitation on the BMC fixture (find_tables detects 0 tables on text-positioned tables) is a documented architectural gap noted in both the SUMMARY and the integration test skip message. The CONTEXT.md and the NOTE in the success criteria both accept this limitation for the PoC, treating SC1 as achievable at the code level.

---

_Verified: 2026-04-26T23:00:00Z_
_Verifier: Claude (gsd-verifier)_
