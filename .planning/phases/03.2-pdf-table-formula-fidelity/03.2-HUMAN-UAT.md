---
status: passed
phase: 03.2-pdf-table-formula-fidelity
source:
  - 03.2-VERIFICATION.md
started: 2026-04-26T20:30:00Z
updated: 2026-04-27T00:00:00Z
---

## Current Test

[awaiting human testing]

## Tests

### 1. BMC paper math-glyph passthrough — visual check
expected: Re-run translation job on `.data/jobs/932530eb-eeda-4b0a-986a-7d32f74fa820/source.pdf`. Open output PDF page 3. Confirm math/symbol glyphs (`¼`, etc.) render correctly — no tofu/missing-glyph boxes. Body text on page 3 still translated.
result: passed

### 2. Vector-bordered table cell extraction — needs vector-table fixture
expected: Test fixture is a PDF with a table drawn using actual vector lines (not text-positioned). Confirm each cell becomes its own segment in the review UI and the output PDF preserves columnar structure on translation. Find/produce a fixture (e.g. simple Word→PDF export with a real table) before testing.
result: passed

### 3. Existing PDF regression
expected: Re-run any prior PDF translation jobs (Phase 03 fixtures `d73528f6`, `c457a7ea`). Confirm no regression on caption-vs-image clipping, heading rendering, or layout fidelity from 03.1.
result: passed

## Summary

total: 3
passed: 3
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "Tables in text-positioned academic-paper PDFs (e.g. BMC paper Table 2) preserve columnar structure on translation"
  status: deferred
  reason: "PyMuPDF find_tables() default strategy returns 0 tables when borders are text-positioned (no vector lines). Phase 03.2 wires the per-cell infrastructure but the BMC fixture still falls through to the existing block path. Closing this fully requires a strategy='text' fallback with row/col filters AND a way to handle the resulting tight-rect cells (cells often 4-5pt tall, below the current MIN_RECT_HEIGHT=6 guard). Track as Phase 04+ enhancement."
  severity: minor
  test: 2
  artifacts:
    - path: "backend/src/app/pipeline/pdf/extractor.py"
      issue: "find_tables() called with default strategy only; text-strategy fallback would over-detect on BMC (returns 102x12 mega-table) without additional filters"
  missing:
    - "find_tables(strategy='text') fallback path with col_count/row_count/cell-width filters to avoid false positives"
    - "Per-cell MIN_RECT_HEIGHT relaxation (or alternative dispatch when cells are tight)"
    - "Vector-bordered table fixture for the integration test currently marked skip"
  root_cause: ""
  debug_session: ""
