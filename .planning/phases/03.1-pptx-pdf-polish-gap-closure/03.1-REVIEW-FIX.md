---
phase: 03.1-pptx-pdf-polish-gap-closure
fixed_at: 2026-04-26T13:30:00Z
review_path: .planning/phases/03.1-pptx-pdf-polish-gap-closure/03.1-REVIEW.md
iteration: 2
findings_in_scope: 6
fixed: 6
skipped: 0
status: all_fixed
---

# Phase 03.1: Code Review Fix Report

**Fixed at:** 2026-04-26T13:30:00Z
**Source review:** .planning/phases/03.1-pptx-pdf-polish-gap-closure/03.1-REVIEW.md
**Iteration:** 2 (combined)

**Summary:**
- Findings in scope: 6 (2 Warning + 4 Info)
- Fixed: 6
- Skipped: 0

---

## Iteration 1 — Warnings (fix_scope: critical_warning)

### WR-02: `add_redact_annot(fill=False)` passes wrong type

**Files modified:** `backend/src/app/pipeline/pdf/reassembler.py`
**Commit:** 121fbbc
**Applied fix:** Changed `fill=False` to `fill=None` on the `page.add_redact_annot(rect, ...)` call (line 176). The original `False` value accidentally worked via PyMuPDF C-extension truthiness, but `fill` expects `None` or a color sequence (`OptSeq`). Using `None` is semantically correct and future-proof against PyMuPDF type-checking changes. Updated the inline comment to match.

### WR-01: `_clip_rect_away_from_images` misses images overlapping from the left

**Files modified:** `backend/src/app/pipeline/pdf/reassembler.py`, `backend/tests/pipeline/test_pdf_reassembler.py`
**Commit:** a2b1ddf
**Applied fix:** Replaced the original right-overlap guard (`img.x0 > clipped.x0 and img.x0 < clipped.x1`) with a two-branch structure:
- `if img.x0 > clipped.x0` — image starts inside the rect horizontally: clip right edge to `min(clipped.x1, img.x0)` (right-overlap case, same behavior as before)
- `elif img.x1 < clipped.x1` — image left edge is at or before the text rect's left edge but still intersects: clip left edge to `max(clipped.x0, img.x1)` (left-overlap case, previously unhandled)

The `min`/`max` guards prevent the rect from being inverted when the image fully covers the horizontal span. Also added `test_clip_rect_away_from_images_clips_left_edge` to `test_pdf_reassembler.py` covering the empirical case from the review (`text_rect=(100,100,200,120)`, `image_rect=(50,90,180,130)`), which now correctly clips `x0` to 180.

---

## Iteration 2 — Info findings (fix_scope: all)

### IN-01: Inconsistent overflow flag dict schemas across append sites

**Files modified:** `backend/src/app/pipeline/pdf/reassembler.py`
**Commit:** 99f5fb3
**Applied fix:** Added `"auto_adjusted": False` to the four `overflow=True` flag entries that were missing it: `rect_too_small` (Pass 1 filter), `image_collision` (Pass 3 clip check), `exception` (insert_htmlbox try/except), and `spare_height < 0` (overflow at scale_low=0.7). The `scale < 1.0` entry already carried `"auto_adjusted": True` and was left unchanged. All five overflow entry shapes now have a uniform key set: `segment_id`, `overflow`, `auto_adjusted`, `scale_applied`, plus optional `reason`/`error`/`rect_width`/`rect_height`.

**Verification:** All 7 `test_pdf_reassembler.py` tests pass.

### IN-02: Dead code — `doc` and `page` unused in `test_overflow_db_flag_persisted`

**Files modified:** `backend/tests/pipeline/test_pdf_reassembler.py`
**Commit:** f8c543b
**Applied fix:** Removed the three dead lines (77–79) that created an ephemeral `doc` / `page` / `page.insert_text(...)` that was never passed to any function. `doc2` already contained identical content and was the actual document used throughout the test. The test logic and assertions are unchanged.

**Verification:** All 7 `test_pdf_reassembler.py` tests pass.

### IN-03: `pytest-cov` declared in both `[project.dependencies]` and `[project.optional-dependencies]`

**Files modified:** `backend/pyproject.toml`
**Commit:** 48e49ce
**Applied fix:** Removed `"pytest-cov>=7.1.0"` from `[project.dependencies]`. The entry in `[project.optional-dependencies] test` (`"pytest-cov>=5.0"`) is sufficient and correct — `pytest-cov` is a test-only tool and must not be installed in production environments. Also removes the conflicting version floor (`>=7.1.0` vs `>=5.0`) that could cause resolution conflicts.

**Verification:** All 100 `tests/pipeline/` tests pass.

### IN-04: Inline imports of `MSO_SHAPE_TYPE` and `is_smartart` inside `_write_back_shapes`

**Files modified:** `backend/src/app/pipeline/pptx/reassembler.py`
**Commit:** a5da1fe
**Applied fix:** Hoisted `from pptx.enum.shapes import MSO_SHAPE_TYPE` and `from app.pipeline.pptx.smartart import is_smartart` to module-level imports (inserted after `from pptx.enum.text import MSO_AUTO_SIZE`). Removed the two inline import statements from `_write_back_shapes` along with their `# noqa: PLC0415` suppressions. No circular import risk: `smartart.py` has no imports from `pptx.reassembler`.

**Verification:** All 100 `tests/pipeline/` tests pass.

---

_Fixed: 2026-04-26T13:30:00Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
